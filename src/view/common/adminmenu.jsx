import React from 'react';

const MenuContent = ({menuaccess}) => (
  //  console.log(menuaccess.state.data);
     <div id="menu-root">

      <ul>
          <li>
            <div>
             {/* {this.menuaccess.options.map(item=>
              <li>{item.name}</li> */}
              )}
            </div>
          </li>
        </ul> 
    
 </div>
);

export default MenuContent;